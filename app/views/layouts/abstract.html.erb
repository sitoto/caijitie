<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= (yield(:title) && !yield(:title).empty? ? yield(:title) : "") + " - 脱水吧" %></title>
  <%= metamagic :title => "帖子在线脱水小工具",
                :description => "第一在线采集工具，这是一个可以在线看百度贴吧、天涯社区、豆瓣话题、豆瓣直播的脱水版的帖子工具。",
                :keywords => %w(脱水吧 脱水版 百度贴吧 只看楼主 天涯易读 豆瓣脱水) %>
  <%= render "layouts/stylesheets" %>
  <%= yield(:head) -%>
  <%= google_analytics  %>
</head>
<body>

  <%= yield :pre_content %>
  <%= yield :layout%>
  <%= yield :post_content %>
  <%= debug(params) if Rails.env.development? %>

</body>
</html>
