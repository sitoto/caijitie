<%- title "查找#{@search}帖子列表." -%>

<section class="post-list">
  <%- if @search && @search.length > 0 -%>
    <%= render(:partial => "share/list", :object => @search) %>
    <%= paginate @search, :outer_window => 4, :inner_window => 4 -%>
  <%- else -%>
  <p>没有内容。.</p>
  <%- end -%>
</section>